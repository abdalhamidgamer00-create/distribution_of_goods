# Copy this file to secrets.toml and replace the placeholder values
# with strong, unique passwords before running the application.
# انسخ هذا الملف إلى secrets.toml وغيّر كلمات المرور قبل التشغيل.

[passwords]
admin = "CHANGE_ME_use_a_strong_password"
user = "CHANGE_ME_use_a_strong_password"
