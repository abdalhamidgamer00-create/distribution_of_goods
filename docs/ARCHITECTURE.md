# معمارية المشروع والقواعد البرمجية (Architecture & Rules)

يتبع هذا المشروع مبادئ **Clean Architecture** و **Domain-Driven Design (DDD)** لضمان القابلية للصيانة، التوسع، والاختبار.

## 🏗️ هيكلية المجلدات (Source Structure)

يتم تقسيم الكود في مجلد `src/` كالتالي:

1.  **`src/domain/`**: جوهر النظام (The Core). يحتوي على:
    *   **Models**: الكيانات الأساسية (مثل `Branch`, `Product`).
    *   **Services**: منطق العمل الصرف (Business Logic) مثل حسابات التوزيع.
    *   **Exceptions**: الاستثناءات الخاصة بالمجال.
    *   *القاعدة: لا يعتمد هذا المجلد على أي شيء خارجي.*

2.  **`src/application/`**: طبقة التنسيق (Orchestration).
    *   **Use Cases**: تنفيذ العمليات الوظيفية.
    *   **Pipeline**: مراحل معالجة البيانات.
    *   **Ports**: واجهات (Interfaces) للتعامل مع البنية التحتية.

3.  **`src/infrastructure/`**: التفاصيل التقنية.
    *   **Excel**: التعامل مع ملفات الإكسل.
    *   **Adapters**: تنفيذ الـ Ports (مثل الوصول للبيانات).
    *   **Converters**: تحويل البيانات بين الطبقات.

4.  **`src/presentation/`**: واجهة المستخدم.
    *   **GUI**: واجهة Streamlit.
    *   **CLI**: واجهة سطر الأوامر.

5.  **`src/shared/`**: الأدوات المساعدة المشتركة (Utilities & Constants).

---

## 📜 القواعد الذهبية (The Golden Rules)

### 1. قاعدة "عدم الاختصار" (Zero Abbreviations Policy)
*   **ممنوع** استخدام اختصارات مثل `calc`, `tmp`, `val`, `auth`.
*   **يجب** استخدام أسماء كاملة واضحة مثل `calculator`, `temporary_data`, `validated_value`, `authentication`.
*   *الهدف: الكود يجب أن يقرأ كاللغة الإنجليزية.*

### 2. قاعدة 100/20/80
*   **100%**: التزام كامل بالمعمارية (Clean Architecture).
*   **20%**: الحد الأقصى لحجم الدوال والتعقيد.
*   **80%**: الحد الأدنى للتغطية الاختبارية (Test Coverage) المطلوبة.

### 3. استقلال المناطق (Separation of Concerns)
*   منطق الحسابات يجب أن يبقى في الـ `domain`.
*   التعامل مع ملفات Excel يجب أن يبقى في الـ `infrastructure`.
*   واجهات المستخدم لا تعرف شيئاً عن كيفية حساب البيانات، بل تطلبها فقط.

---

## 🛠️ كيف تبدأ بالتطوير؟
1.  إذا أردت إضافة منطق جديد، ابدأ بالـ `domain`.
2.  إذا أردت إضافة ميزة للمستخدم، أضف Use Case في الـ `application` ثم اربطها بالـ `presentation`.
3.  تأكد دائماً من تشغيل الاختبارات عبر `pytest` قبل رفع أي تغيير.
